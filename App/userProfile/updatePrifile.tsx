import React, {Component} from 'react';
import {Text} from 'react-native';

export default class PofileSave extends Component{
    render(){
        return (
            <Text>Profile save</Text>
        )
    }
}